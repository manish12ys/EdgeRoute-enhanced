services:
  - type: web
    name: edgeroute
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -m flask db upgrade
      python create_default_avatar.py
    startCommand: gunicorn "run:app"
    plan: free

    envVars:
      - key: FLASK_APP
        value: run.py
      - key: FLASK_ENV
        value: production
      - key: FLASK_CONFIG
        value: production
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: edgeroute-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      # Auth0 configuration
      - key: USE_AUTH0
        value: "true"
      - key: AUTH0_DOMAIN
        sync: false
      - key: AUTH0_CLIENT_ID
        sync: false
      - key: AUTH0_CLIENT_SECRET
        sync: false
      - key: AUTH0_AUDIENCE
        sync: false
      - key: AUTH0_CALLBACK_URL
        value: https://edgeroute.onrender.com/auth/callback

      # Appwrite configuration
      - key: USE_APPWRITE
        value: "false"
      - key: APPWRITE_ENDPOINT
        value: https://cloud.appwrite.io/v1
      - key: APPWRITE_PROJECT_ID
        sync: false
      - key: APPWRITE_API_KEY
        sync: false
      - key: APPWRITE_DATABASE_ID
        value: edgeroute

databases:
  - name: edgeroute-db
    databaseName: edgeroute
    user: edgeroute
    plan: free
